import React from 'react';
import ReactDOM from 'react-dom';
import MainApp from './App'; // Import your main component (App or named as MainApp)
 
ReactDOM.render(
  <React.StrictMode>
    <MainApp />
  </React.StrictMode>,
  document.getElementById('root')
);
